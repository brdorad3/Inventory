extends layout 

block content 
    h1 Category list
    for category in categories 
        ul 
            li 
                a(href=category.url) #{category.name}
    a(href="/items/cat_list/create") Create a new category?